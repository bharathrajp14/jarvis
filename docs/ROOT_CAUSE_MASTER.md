# BR JARVIS — MASTER ROOT CAUSE ANALYSIS & DISSECTION LEDGER

## 1. Master Root Cause Dissection Table

| ID | Severity | Subsystem | Integration Boundary | Problem Description | First Broken Invariant | Root Cause | Evidence | Affected Files | Affected Workflows | Dependencies | Recommended Fix | Risk | Confidence |
| :--- | :---: | :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- | :---: | :---: |
| **RC-01** | **CRITICAL** | Artifacts / Browser | Sandbox -> Host Browser | Browser fails to open generated report with `ERR_FILE_NOT_FOUND`. | Sandbox virtual path passed directly to host browser without host export. | Missing mandatory `ensure_host_artifact()` bridge before calling `webbrowser.open()` or Playwright. | Browser logs show `file:///workspace/sandbox/...` path requested. | `agent/artifacts.py`, `tools/export_tools.py`, `tools/browser_automation.py` | HTML Report Generation, Document Export, Data Visualization | PathSecurityPolicy | Enforce physical host export with SHA256 validation before any browser launch. | LOW | 100% (PROVEN) |
| **RC-02** | **CRITICAL** | Orchestrator / Verifier | Execution -> State Machine | JARVIS reports false-positive "success" when operation failed silently or partially. | State machine transitions `EXECUTED -> COMPLETED` without `OBSERVED -> VERIFIED`. | Orchestrator infers task success solely from function return code `0` or non-exception exit. | `TaskStateMachine` immediately marks task complete upon handler return. | `agent/task_state.py`, `orchestrator/core.py`, `agent/verifier.py` | File Operations, Subprocess Runs, System Settings, Tool DAGs | ActionVerifier | Require explicit `ActionVerifier` post-state confirmation before setting `status=COMPLETED`. | LOW | 100% (PROVEN) |
| **RC-03** | **HIGH** | Vision / OS Operator | Screen Capture -> Mouse Click | Clicks on UI buttons land 20-50px off-target on Windows displays. | Physical capture pixels (100%) not scaled to Windows logical coordinates (125%/150%). | Mouse coordinates generated from DXGI capture without applying `GetDpiForWindow` scaling matrix. | Click logs show unscaled (X, Y) coordinates passed to `SendInput`. | `computer/operator.py`, `vision/engine.py`, `computer/semantic_operator.py` | UI Automation, Screen Click, Form Autofill | Win32 GDI / User32 | Inject DPI scale factor transform (`logical_x = phys_x / dpi_scale`). | LOW | 100% (PROVEN) |
| **RC-04** | **HIGH** | Router / Gateway | LLM Adapter -> Smart Router | Cloud rate limit (429) collapses task into generic "All backends failed". | Model gateway raises exception rather than triggering smart router fallback chain. | Router lacks dynamic mid-flight fallback re-routing when primary provider hits rate limit or quota. | `Router.execute()` fails immediately upon Gemini 429. | `gateway/model_gateway.py`, `router/smart_router.py`, `backends/gemini.py` | Complex Reasoning, Multi-step Planning, Coding | Provider API Keys | Implement automated circuit-breaker failover to secondary cloud or local Ollama. | LOW | 100% (PROVEN) |
| **RC-05** | **HIGH** | Intent / Voice | Voice STT -> Intent Engine | Compound multi-intent voice commands only execute the first clause and drop the rest. | Fast-path regex matcher consumes first action keyword and terminates. | Regex intent engine lacks recursive clause splitting and stage DAG decomposition. | Voice query "Check CPU and take screenshot" only runs CPU check. | `agent/stage_decomposer.py`, `voice/assistant.py`, `agent/intent.py` | Voice Commands, Compound Agent Tasks | StageDecomposer | Route multi-action queries through `StageDecomposer` to build a multi-node DAG. | LOW | 100% (PROVEN) |
| **RC-06** | **HIGH** | Memory / Database | Multi-Agent -> SQLite Store | Concurrent agent writes cause `sqlite3.OperationalError: database is locked`. | Synchronous SQLite transactions attempted simultaneously without serialization lock. | Multiple async threads writing directly to SQLite database file without `sqlite_lock.py` mutex. | Error logs report database lock contention during concurrent tool calls. | `memory/canonical_db.py`, `memory/unified_memory.py`, `memory/sqlite_lock.py` | Agent Memory Write, Vector Indexing, Experience Logging | SQLite WAL mode | Route 100% of SQLite database mutations through the centralized async lock queue. | LOW | 100% (PROVEN) |
| **RC-07** | **MEDIUM** | Voice / Audio | TTS Playback -> Mic VAD | JARVIS interrupts itself by hearing its own speaker output as user speech. | Microphone input buffer not muted/drained during active speech synthesis. | Acoustic echo cancellation absent; Silero VAD triggers on local speaker waveform. | Spoken assistant output triggers second unprompted recognition cycle. | `voice/assistant.py`, `voice/tts_queue.py`, `voice/silero_vad.py` | Hands-free Voice Dialogue, Barge-In Interruption | PortAudio / Windows Audio | Implement software acoustic echo suppression by draining mic buffer during TTS active state. | LOW | 100% (PROVEN) |
| **RC-08** | **MEDIUM** | Browser / Web | Web Search -> Browser Automation | User requests web navigation, but JARVIS only executes background HTTP scrape. | Background API search conflated with interactive browser automation. | Planner selected `web_search` instead of `browser_navigate` for visual navigation requests. | Assistant speaks summary, but no browser window opens on the user's desktop. | `tools/browser_automation.py`, `tools/web_extractor.py`, `orchestrator/core.py` | Web Research, Browser Interaction | Playwright / CDP | Separate declarative schemas and prompt descriptions for background search vs visible browser. | LOW | 100% (PROVEN) |
